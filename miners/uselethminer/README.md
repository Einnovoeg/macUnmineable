# UselethMiner runtime directory

This folder is where the official `UselethMiner` macOS arm64 payload is staged
for local app builds and runtime use.

The source repository intentionally does not commit the downloaded payload.
Install or update it with:

```bash
./scripts/install_uselethminer.sh
```
