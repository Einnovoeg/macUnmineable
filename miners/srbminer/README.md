This directory is reserved for a user-supplied custom secondary miner binary.

The project does not ship an official SRBMiner macOS installer because the
current upstream release stream does not provide a normal macOS release asset.

If you have a compatible custom build, place it here as `SRBMiner-MULTI` or
point the app at an absolute path from the Setup panel.
